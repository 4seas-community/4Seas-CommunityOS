# The DSH-managed global pnpm policy rejects brand-new releases; this project
# pins wrangler which can be freshly published, so relax the age gate locally.
minimum-release-age=0dangerously-allow-all-builds=true
strict-dep-builds=false
